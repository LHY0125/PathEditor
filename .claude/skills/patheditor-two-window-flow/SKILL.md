---
name: patheditor-two-window-flow
description: PathEditor 双窗口协作总纲。当用户提到「审核窗口」「开发窗口」「双窗口」「下一阶段开发」「开新特性」「接收开发计划」「交接文档」「审核文档」时使用，用来判定当前会话是哪个窗口、该走哪条流程，然后把控制权交给对应的窗口 skill。
---

# PathEditor 双窗口协作总纲

PathEditor 的开发由一个**审核窗口**和一个**开发窗口**交替推进。两者是**独立会话**——它们之间不共享上下文，**所有交流都通过仓库里的文档完成**。这条约束决定了下面的一切：凡是希望对方知道的事情，必须落到文件里。

本 skill 是路由。判明当前会话的角色后，**立即调用对应的窗口 skill**，不要凭本文件直接开工。

## 判定当前角色

| 用户说什么                         | 当前窗口         | 调用的 skill               |
| ---------------------------------- | ---------------- | -------------------------- |
| 「你是审核窗口」                   | 审核窗口         | `patheditor-review-window` |
| 「你是开发窗口」                   | 开发窗口         | `patheditor-dev-window`    |
| 「更新 scoop」/ 发版后同步包管理器 | 开发窗口         | `patheditor-scoop-release` |
| 含糊 / 未声明                      | **先问，不要猜** | ——                         |

角色由用户宣告，不由任务内容推断。用户说「审核这次开发」可能是在审核窗口派开发窗口的活，也可能是在开发窗口自查——问清楚再走。

## 一条特性的完整生命周期

```text
审核窗口                                    开发窗口
────────                                    ────────
①与用户确认需求与细节
        │
        ▼
②写 spec + 计划 ──────── 计划文档 ────────▶ ③a 读计划、提异议
        ▲                                              │
        │                                              ▼
        └────────── 异议与打磨（可多轮）◀─────── ③b 打磨
                                                       │
                                                       ▼
                                              ③c 开发（质量门全绿）
                                                       │
        ◀────────── 开发回执 ─────────────────────────┘
        │
        ▼
④审核代码与架构
        │
        ▼
⑤审查报告 ─────────────────────────────▶ 接收、整改
        ▲                                              │
        │                                              ▼
        └────────── 复审回执 ◀──────────────── 写回执 / 或退回
```

一个来回**只走一轮**：审查报告里的问题在开发窗口整改后由回执回复，审核窗口下一轮再决定是否复验。不要在单轮里反复拉锯——每一轮都必须落一个文件。

## 交付物必须固定到这些路径

两个窗口都**只认这些路径**，不要另起名字或换目录：

| 交付物               | 路径                                                             | 作者     |
| -------------------- | ---------------------------------------------------------------- | -------- |
| 设计文档 (spec)      | `docs/superpowers/specs/YYYY-MM-DD-<feature>-design.md`          | 审核窗口 |
| 实施计划             | `docs/superpowers/plans/YYYY-MM-DD-<feature>-implementation.md`  | 审核窗口 |
| 开发回执             | `docs/审核和开发/YYYY.MM.DD/PathEditor-<feature>开发回执.md`     | 开发窗口 |
| 审查报告             | `docs/审核和开发/YYYY.MM.DD/PathEditor-<feature>审查报告.md`     | 审核窗口 |
| 复审回执             | `docs/审核和开发/YYYY.MM.DD/PathEditor-<feature>复审回执.md`     | 开发窗口 |
| 闭环测试记录（可选） | `docs/审核和开发/YYYY.MM.DD/PathEditor-<feature>闭环测试记录.md` | 开发窗口 |

日期用当前系统日期（Asia/Shanghai）。目录日期与文档内日期必须一致。

这些目录里已有先例，**动手写之前先读同目录下最近的一份**，照它的章节结构写——比任何模板都准。历史位置见 `docs/审核和开发/2026.09.17/` 与 `docs/审核和开发/2026.09.18/`。

## 核心细节必须落盘为交接文档（强制）

用户是**两个窗口之间的唯一信道**：他手工复制一个窗口的输出粘贴给另一个窗口。复制必然有遗漏、截断、压缩——**凡是没有落进文件的细节，就等于没传递**。对方看不到，只能凭残缺信息行动，或反复回头重读文件，两边的进度就此错位。

因此，**核心细节除了在对话里显示给用户看，还必须同时写进交接文档**。对话输出是给用户即时阅读的，文档才是两个窗口之间的权威信道。

### 什么算「核心细节」

只要属于下面任一类，就必须落盘：

- **判定与裁断**：结论、理由、依据的代码位置或实验证据
- **异议与回复**：提出的问题、对方的回复、最终取哪一个方案及为什么
- **代码事实**：文件:行、函数签名、类型定义、字段语义——尤其是「计划里写的与仓库实际不符」这类修正
- **阻塞与风险**：发现的问题、影响范围、是否阻塞开工
- **偏离记录**：计划原文 / 实际实现 / 处理方式
- **未覆盖项**：没做的事、没验证的场景、已知残余风险
- **待决事项**：需要对方或用户拍板的问题（含已给的选项与推荐项）

反之，寒暄、进度播报（「正在跑测试」）、可从文件直接读到的内容**不必**重复落盘。

### 落盘时机与路径

**每一轮交流结束时，把该轮的核心细节写成一份交接文档**，不要攒到最后，也不要只在最后一轮补写：

| 轮次                                      | 交接文档   | 路径                                                           |
| ----------------------------------------- | ---------- | -------------------------------------------------------------- |
| 计划核对（开发窗口提异议 / 审核窗口裁断） | 核对轮交接 | `docs/审核和开发/YYYY.MM.DD/PathEditor-<feature>核对轮交接.md` |
| 开发完成                                  | 开发回执   | `docs/审核和开发/YYYY.MM.DD/PathEditor-<feature>开发回执.md`   |
| 审核完成                                  | 审查报告   | `docs/审核和开发/YYYY.MM.DD/PathEditor-<feature>审查报告.md`   |
| 整改完成                                  | 复审回执   | `docs/审核和开发/YYYY.MM.DD/PathEditor-<feature>复审回执.md`   |

已有固定文档（回执 / 审查报告）的轮次，**把核心细节写进该文档的对应小节**，不另开文件；只有尚无归属文档的轮次（如计划核对）才新建「核对轮交接」。

### 写法要求

- **每条细节自带定位**：写清是哪个任务、哪个文件、哪一行，不要写「前面提到的那个问题」。
- **判定必须带理由与证据**：不写「建议改 X」，写「建议改 X，因为 <代码位置/实验输出/失败信息>」。
- **未决项显式标注**：用「**待裁断**」开头，列出选项与推荐项，让对方能直接作答。
- **对话里说了什么，文档里就要有什么**：若某个结论只出现在聊天输出里，视为该轮**未完成**。

> 这条规则的目的不是增加文书量，而是让两个窗口**只靠仓库就能完整重建对方的推理过程**——用户漏复制一段，对方仍能从文档里拿到全部关键信息，不必回头重读。

## 两个窗口都要守的硬约束

来自仓库 `CLAUDE.md` / `AGENTS.md`，任何一轮都不得违反：

- **不删除任何文件**，未经用户书面同意。
- **不提交、不推送、不升级版本号**，未经用户明确要求。提交是否获授权按轮次重新确认，上一轮的授权不延续。
- **不写真实注册表**，除非用户显式授权；授权后必须记录备份、操作前后快照与回滚结果。
- **E2E 用 mock IPC**，永远不碰真实注册表。
- **`PathEditor` CLI 单测必须用 `cargo test -p patheditor-cli --bins`**（bin-only crate，`--lib` 会报 `no library targets found`）。
- 聊天用中文，代码注释用中文。

## 交付纪律

- 两份文档（`AGENTS.md` 与 `CLAUDE.md`）必须**字节级一致**——改一份就同步另一份。
- 任何「绿灯」「通过」「已修复」的结论，必须附**实际执行过的命令与输出**。没跑过的不要写。
- 未覆盖项、已知保留项、残余风险**如实列出**，不粉饰。历史文档里 `### 未覆盖项（如实列出）`、`[非阻断]` 这类小节是刻意的，保留这个习惯。

## 越界即停

出现下面任一情况，停下来问用户，不要自作主张：

- 需要删除文件、推送、升级版本、写真实注册表
- 特性范围发生变化，或发现计划本身有结构性缺陷
- 两个窗口对同一条需求的理解冲突，文档里没有权威答案
